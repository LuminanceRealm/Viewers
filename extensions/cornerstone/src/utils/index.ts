import { handleSegmentChange } from './segmentUtils';
import * as printSheet from './printSheet';

const utils = {
  handleSegmentChange,
  /** Hojas de impresión (imágenes e informes) compartidas con las extensiones NUBIX. */
  printSheet,
};

export default utils;
